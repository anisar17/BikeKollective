import 'package:flutter/material.dart';

class ExploreBikesScreen extends StatelessWidget {
  const ExploreBikesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Home Screen'),
    );
  }
}
