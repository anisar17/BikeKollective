import 'package:flutter/material.dart';

class CurrentRideScreen extends StatelessWidget {
  const CurrentRideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Current Ride Screen'),
    );
  }
}
